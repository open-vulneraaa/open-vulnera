---
title: Examples
description: Get started by copying these code snippets into your terminal, a `.py` file, or a Jupyter notebook.
---

<CardGroup>

<Card
  title="Interactive demo"
  icon="gamepad-modern"
  iconType="solid"
  href="https://colab.research.google.com/drive/1WKmRXZgsErej2xUriKzxrEAXdxMSgWbb?usp=sharing"
>
  Try Open Vulnera without installing anything on your computer
</Card>

<Card
  title="Example voice interface"
  icon="circle"
  iconType="solid"
  href="https://colab.research.google.com/drive/1NojYGHDgxH6Y1G1oxThEBBb2AtyODBIK"
>
  An example implementation of Open Vulnera's streaming capabilities
</Card>

</CardGroup>

---

### Interactive Chat

To start an interactive chat in your terminal, either run `vulnera` from the command line:

```shell
vulnera
```

Or `vulnera.chat()` from a .py file:

```python
vulnera.chat()
```

---

### Programmatic Chat

For more precise control, you can pass messages directly to `.chat(message)` in Python:

```python
vulnera.chat("Add subtitles to all videos in /videos.")

# ... Displays output in your terminal, completes task ...

vulnera.chat("These look great but can you make the subtitles bigger?")

# ...
```

---

### Start a New Chat

In your terminal, Open Vulnera behaves like ChatGPT and will not remember previous conversations. Simply run `vulnera` to start a new chat:

```shell
vulnera
```

In Python, Open Vulnera remembers conversation history. If you want to start fresh, you can reset it:

```python
vulnera.messages = []
```

---

### Save and Restore Chats

In your terminal, Open Vulnera will save previous conversations to `<your application directory>/Open Vulnera/conversations/`.

You can resume any of them by running `--conversations`. Use your arrow keys to select one , then press `ENTER` to resume it.

```shell
vulnera --conversations
```

In Python, `vulnera.chat()` returns a List of messages, which can be used to resume a conversation with `vulnera.messages = messages`:

```python
# Save messages to 'messages'
messages = vulnera.chat("My name is Killian.")

# Reset vulnera ("Killian" will be forgotten)
vulnera.messages = []

# Resume chat from 'messages' ("Killian" will be remembered)
vulnera.messages = messages
```

---

### Configure Default Settings

We save default settings to a profile which can be edited by running the following command:

```shell
vulnera --profiles
```

You can use this to set your default language model, system message (custom instructions), max budget, etc.

<Info>
  **Note:** The Python library will also inherit settings from the default
  profile file. You can change it by running `vulnera --profiles` and
  editing `default.yaml`.
</Info>

---

### Customize System Message

In your terminal, modify the system message by [editing your configuration file as described here](#configure-default-settings).

In Python, you can inspect and configure Open Vulnera's system message to extend its functionality, modify permissions, or give it more context.

```python
vulnera.system_message += """
Run shell commands with -y so the user doesn't have to confirm them.
"""
print(vulnera.system_message)
```

---

### Change your Language Model

Open Vulnera uses [LiteLLM](https://docs.litellm.ai/docs/providers/) to connect to language models.

You can change the model by setting the model parameter:

```shell
vulnera --model gpt-3.5-turbo
vulnera --model claude-2
vulnera --model command-nightly
```

In Python, set the model on the object:

```python
vulnera.llm.model = "gpt-3.5-turbo"
```

[Find the appropriate "model" string for your language model here.](https://docs.litellm.ai/docs/providers/)
